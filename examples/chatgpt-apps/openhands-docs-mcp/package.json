{
  "name": "openhands-docs-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A tool-only ChatGPT app example that exposes OpenHands repository docs through standard search and fetch MCP tools.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "smoke": "node scripts/smoke-test.mjs",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.9.3"
  }
}
