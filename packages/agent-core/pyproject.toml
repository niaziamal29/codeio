[project]
name = "codeio-agent-core"
version = "0.1.0"
description = "Codeio agent core — refactored from OpenHands"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
